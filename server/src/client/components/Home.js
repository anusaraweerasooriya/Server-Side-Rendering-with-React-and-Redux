import React from "react";

const Home = () => {
  return (
    <div>
      <div>Home components are here</div>
      <button onClick={() => console.log("Hi there!")}>Press me!</button>
    </div>
  );
};

export default Home;
